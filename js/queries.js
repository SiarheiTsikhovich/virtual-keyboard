export const queryTextArea = () => document.querySelector('textarea');
export const queryKeyboard = () => document.querySelector('.keyboard');
export const queryButton = (keyCode) => document.querySelector(`button.${keyCode}`);
